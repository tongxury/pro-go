// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"log"

	"store/app/payment/internal/data/repo/ent/migrate"

	"store/app/payment/internal/data/repo/ent/creditconsume"
	"store/app/payment/internal/data/repo/ent/creditrecharge"
	"store/app/payment/internal/data/repo/ent/payment"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// CreditConsume is the client for interacting with the CreditConsume builders.
	CreditConsume *CreditConsumeClient
	// CreditRecharge is the client for interacting with the CreditRecharge builders.
	CreditRecharge *CreditRechargeClient
	// Payment is the client for interacting with the Payment builders.
	Payment *PaymentClient
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	cfg := config{log: log.Println, hooks: &hooks{}, inters: &inters{}}
	cfg.options(opts...)
	client := &Client{config: cfg}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.CreditConsume = NewCreditConsumeClient(c.config)
	c.CreditRecharge = NewCreditRechargeClient(c.config)
	c.Payment = NewPaymentClient(c.config)
}

type (
	// config is the configuration for the client and its builder.
	config struct {
		// driver used for executing database requests.
		driver dialect.Driver
		// debug enable a debug logging.
		debug bool
		// log used for logging on debug mode.
		log func(...any)
		// hooks to execute on mutations.
		hooks *hooks
		// interceptors to execute on queries.
		inters *inters
	}
	// Option function to configure the client.
	Option func(*config)
)

// options applies the options on the config object.
func (c *config) options(opts ...Option) {
	for _, opt := range opts {
		opt(c)
	}
	if c.debug {
		c.driver = dialect.Debug(c.driver, c.log)
	}
}

// Debug enables debug logging on the ent.Driver.
func Debug() Option {
	return func(c *config) {
		c.debug = true
	}
}

// Log sets the logging function for debug mode.
func Log(fn func(...any)) Option {
	return func(c *config) {
		c.log = fn
	}
}

// Driver configures the client driver.
func Driver(driver dialect.Driver) Option {
	return func(c *config) {
		c.driver = driver
	}
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, errors.New("ent: cannot start a transaction within a transaction")
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:            ctx,
		config:         cfg,
		CreditConsume:  NewCreditConsumeClient(cfg),
		CreditRecharge: NewCreditRechargeClient(cfg),
		Payment:        NewPaymentClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, errors.New("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:            ctx,
		config:         cfg,
		CreditConsume:  NewCreditConsumeClient(cfg),
		CreditRecharge: NewCreditRechargeClient(cfg),
		Payment:        NewPaymentClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		CreditConsume.
//		Query().
//		Count(ctx)
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	c.CreditConsume.Use(hooks...)
	c.CreditRecharge.Use(hooks...)
	c.Payment.Use(hooks...)
}

// Intercept adds the query interceptors to all the entity clients.
// In order to add interceptors to a specific client, call: `client.Node.Intercept(...)`.
func (c *Client) Intercept(interceptors ...Interceptor) {
	c.CreditConsume.Intercept(interceptors...)
	c.CreditRecharge.Intercept(interceptors...)
	c.Payment.Intercept(interceptors...)
}

// Mutate implements the ent.Mutator interface.
func (c *Client) Mutate(ctx context.Context, m Mutation) (Value, error) {
	switch m := m.(type) {
	case *CreditConsumeMutation:
		return c.CreditConsume.mutate(ctx, m)
	case *CreditRechargeMutation:
		return c.CreditRecharge.mutate(ctx, m)
	case *PaymentMutation:
		return c.Payment.mutate(ctx, m)
	default:
		return nil, fmt.Errorf("ent: unknown mutation type %T", m)
	}
}

// CreditConsumeClient is a client for the CreditConsume schema.
type CreditConsumeClient struct {
	config
}

// NewCreditConsumeClient returns a client for the CreditConsume from the given config.
func NewCreditConsumeClient(c config) *CreditConsumeClient {
	return &CreditConsumeClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `creditconsume.Hooks(f(g(h())))`.
func (c *CreditConsumeClient) Use(hooks ...Hook) {
	c.hooks.CreditConsume = append(c.hooks.CreditConsume, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `creditconsume.Intercept(f(g(h())))`.
func (c *CreditConsumeClient) Intercept(interceptors ...Interceptor) {
	c.inters.CreditConsume = append(c.inters.CreditConsume, interceptors...)
}

// Create returns a builder for creating a CreditConsume entity.
func (c *CreditConsumeClient) Create() *CreditConsumeCreate {
	mutation := newCreditConsumeMutation(c.config, OpCreate)
	return &CreditConsumeCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of CreditConsume entities.
func (c *CreditConsumeClient) CreateBulk(builders ...*CreditConsumeCreate) *CreditConsumeCreateBulk {
	return &CreditConsumeCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for CreditConsume.
func (c *CreditConsumeClient) Update() *CreditConsumeUpdate {
	mutation := newCreditConsumeMutation(c.config, OpUpdate)
	return &CreditConsumeUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *CreditConsumeClient) UpdateOne(cc *CreditConsume) *CreditConsumeUpdateOne {
	mutation := newCreditConsumeMutation(c.config, OpUpdateOne, withCreditConsume(cc))
	return &CreditConsumeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *CreditConsumeClient) UpdateOneID(id int64) *CreditConsumeUpdateOne {
	mutation := newCreditConsumeMutation(c.config, OpUpdateOne, withCreditConsumeID(id))
	return &CreditConsumeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for CreditConsume.
func (c *CreditConsumeClient) Delete() *CreditConsumeDelete {
	mutation := newCreditConsumeMutation(c.config, OpDelete)
	return &CreditConsumeDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *CreditConsumeClient) DeleteOne(cc *CreditConsume) *CreditConsumeDeleteOne {
	return c.DeleteOneID(cc.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *CreditConsumeClient) DeleteOneID(id int64) *CreditConsumeDeleteOne {
	builder := c.Delete().Where(creditconsume.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &CreditConsumeDeleteOne{builder}
}

// Query returns a query builder for CreditConsume.
func (c *CreditConsumeClient) Query() *CreditConsumeQuery {
	return &CreditConsumeQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeCreditConsume},
		inters: c.Interceptors(),
	}
}

// Get returns a CreditConsume entity by its id.
func (c *CreditConsumeClient) Get(ctx context.Context, id int64) (*CreditConsume, error) {
	return c.Query().Where(creditconsume.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *CreditConsumeClient) GetX(ctx context.Context, id int64) *CreditConsume {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *CreditConsumeClient) Hooks() []Hook {
	return c.hooks.CreditConsume
}

// Interceptors returns the client interceptors.
func (c *CreditConsumeClient) Interceptors() []Interceptor {
	return c.inters.CreditConsume
}

func (c *CreditConsumeClient) mutate(ctx context.Context, m *CreditConsumeMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&CreditConsumeCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&CreditConsumeUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&CreditConsumeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&CreditConsumeDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown CreditConsume mutation op: %q", m.Op())
	}
}

// CreditRechargeClient is a client for the CreditRecharge schema.
type CreditRechargeClient struct {
	config
}

// NewCreditRechargeClient returns a client for the CreditRecharge from the given config.
func NewCreditRechargeClient(c config) *CreditRechargeClient {
	return &CreditRechargeClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `creditrecharge.Hooks(f(g(h())))`.
func (c *CreditRechargeClient) Use(hooks ...Hook) {
	c.hooks.CreditRecharge = append(c.hooks.CreditRecharge, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `creditrecharge.Intercept(f(g(h())))`.
func (c *CreditRechargeClient) Intercept(interceptors ...Interceptor) {
	c.inters.CreditRecharge = append(c.inters.CreditRecharge, interceptors...)
}

// Create returns a builder for creating a CreditRecharge entity.
func (c *CreditRechargeClient) Create() *CreditRechargeCreate {
	mutation := newCreditRechargeMutation(c.config, OpCreate)
	return &CreditRechargeCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of CreditRecharge entities.
func (c *CreditRechargeClient) CreateBulk(builders ...*CreditRechargeCreate) *CreditRechargeCreateBulk {
	return &CreditRechargeCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for CreditRecharge.
func (c *CreditRechargeClient) Update() *CreditRechargeUpdate {
	mutation := newCreditRechargeMutation(c.config, OpUpdate)
	return &CreditRechargeUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *CreditRechargeClient) UpdateOne(cr *CreditRecharge) *CreditRechargeUpdateOne {
	mutation := newCreditRechargeMutation(c.config, OpUpdateOne, withCreditRecharge(cr))
	return &CreditRechargeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *CreditRechargeClient) UpdateOneID(id int64) *CreditRechargeUpdateOne {
	mutation := newCreditRechargeMutation(c.config, OpUpdateOne, withCreditRechargeID(id))
	return &CreditRechargeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for CreditRecharge.
func (c *CreditRechargeClient) Delete() *CreditRechargeDelete {
	mutation := newCreditRechargeMutation(c.config, OpDelete)
	return &CreditRechargeDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *CreditRechargeClient) DeleteOne(cr *CreditRecharge) *CreditRechargeDeleteOne {
	return c.DeleteOneID(cr.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *CreditRechargeClient) DeleteOneID(id int64) *CreditRechargeDeleteOne {
	builder := c.Delete().Where(creditrecharge.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &CreditRechargeDeleteOne{builder}
}

// Query returns a query builder for CreditRecharge.
func (c *CreditRechargeClient) Query() *CreditRechargeQuery {
	return &CreditRechargeQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeCreditRecharge},
		inters: c.Interceptors(),
	}
}

// Get returns a CreditRecharge entity by its id.
func (c *CreditRechargeClient) Get(ctx context.Context, id int64) (*CreditRecharge, error) {
	return c.Query().Where(creditrecharge.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *CreditRechargeClient) GetX(ctx context.Context, id int64) *CreditRecharge {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *CreditRechargeClient) Hooks() []Hook {
	return c.hooks.CreditRecharge
}

// Interceptors returns the client interceptors.
func (c *CreditRechargeClient) Interceptors() []Interceptor {
	return c.inters.CreditRecharge
}

func (c *CreditRechargeClient) mutate(ctx context.Context, m *CreditRechargeMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&CreditRechargeCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&CreditRechargeUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&CreditRechargeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&CreditRechargeDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown CreditRecharge mutation op: %q", m.Op())
	}
}

// PaymentClient is a client for the Payment schema.
type PaymentClient struct {
	config
}

// NewPaymentClient returns a client for the Payment from the given config.
func NewPaymentClient(c config) *PaymentClient {
	return &PaymentClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `payment.Hooks(f(g(h())))`.
func (c *PaymentClient) Use(hooks ...Hook) {
	c.hooks.Payment = append(c.hooks.Payment, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `payment.Intercept(f(g(h())))`.
func (c *PaymentClient) Intercept(interceptors ...Interceptor) {
	c.inters.Payment = append(c.inters.Payment, interceptors...)
}

// Create returns a builder for creating a Payment entity.
func (c *PaymentClient) Create() *PaymentCreate {
	mutation := newPaymentMutation(c.config, OpCreate)
	return &PaymentCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Payment entities.
func (c *PaymentClient) CreateBulk(builders ...*PaymentCreate) *PaymentCreateBulk {
	return &PaymentCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Payment.
func (c *PaymentClient) Update() *PaymentUpdate {
	mutation := newPaymentMutation(c.config, OpUpdate)
	return &PaymentUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *PaymentClient) UpdateOne(pa *Payment) *PaymentUpdateOne {
	mutation := newPaymentMutation(c.config, OpUpdateOne, withPayment(pa))
	return &PaymentUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *PaymentClient) UpdateOneID(id int64) *PaymentUpdateOne {
	mutation := newPaymentMutation(c.config, OpUpdateOne, withPaymentID(id))
	return &PaymentUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Payment.
func (c *PaymentClient) Delete() *PaymentDelete {
	mutation := newPaymentMutation(c.config, OpDelete)
	return &PaymentDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *PaymentClient) DeleteOne(pa *Payment) *PaymentDeleteOne {
	return c.DeleteOneID(pa.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *PaymentClient) DeleteOneID(id int64) *PaymentDeleteOne {
	builder := c.Delete().Where(payment.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &PaymentDeleteOne{builder}
}

// Query returns a query builder for Payment.
func (c *PaymentClient) Query() *PaymentQuery {
	return &PaymentQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypePayment},
		inters: c.Interceptors(),
	}
}

// Get returns a Payment entity by its id.
func (c *PaymentClient) Get(ctx context.Context, id int64) (*Payment, error) {
	return c.Query().Where(payment.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *PaymentClient) GetX(ctx context.Context, id int64) *Payment {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *PaymentClient) Hooks() []Hook {
	return c.hooks.Payment
}

// Interceptors returns the client interceptors.
func (c *PaymentClient) Interceptors() []Interceptor {
	return c.inters.Payment
}

func (c *PaymentClient) mutate(ctx context.Context, m *PaymentMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&PaymentCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&PaymentUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&PaymentUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&PaymentDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Payment mutation op: %q", m.Op())
	}
}

// hooks and interceptors per client, for fast access.
type (
	hooks struct {
		CreditConsume, CreditRecharge, Payment []ent.Hook
	}
	inters struct {
		CreditConsume, CreditRecharge, Payment []ent.Interceptor
	}
)
