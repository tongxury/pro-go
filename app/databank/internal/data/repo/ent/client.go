// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"log"

	"store/app/databank/internal/data/repo/ent/migrate"

	"store/app/databank/internal/data/repo/ent/userasset"
	"store/app/databank/internal/data/repo/ent/userfile"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// UserAsset is the client for interacting with the UserAsset builders.
	UserAsset *UserAssetClient
	// UserFile is the client for interacting with the UserFile builders.
	UserFile *UserFileClient
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	cfg := config{log: log.Println, hooks: &hooks{}, inters: &inters{}}
	cfg.options(opts...)
	client := &Client{config: cfg}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.UserAsset = NewUserAssetClient(c.config)
	c.UserFile = NewUserFileClient(c.config)
}

type (
	// config is the configuration for the client and its builder.
	config struct {
		// driver used for executing database requests.
		driver dialect.Driver
		// debug enable a debug logging.
		debug bool
		// log used for logging on debug mode.
		log func(...any)
		// hooks to execute on mutations.
		hooks *hooks
		// interceptors to execute on queries.
		inters *inters
	}
	// Option function to configure the client.
	Option func(*config)
)

// options applies the options on the config object.
func (c *config) options(opts ...Option) {
	for _, opt := range opts {
		opt(c)
	}
	if c.debug {
		c.driver = dialect.Debug(c.driver, c.log)
	}
}

// Debug enables debug logging on the ent.Driver.
func Debug() Option {
	return func(c *config) {
		c.debug = true
	}
}

// Log sets the logging function for debug mode.
func Log(fn func(...any)) Option {
	return func(c *config) {
		c.log = fn
	}
}

// Driver configures the client driver.
func Driver(driver dialect.Driver) Option {
	return func(c *config) {
		c.driver = driver
	}
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, errors.New("ent: cannot start a transaction within a transaction")
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:       ctx,
		config:    cfg,
		UserAsset: NewUserAssetClient(cfg),
		UserFile:  NewUserFileClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, errors.New("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:       ctx,
		config:    cfg,
		UserAsset: NewUserAssetClient(cfg),
		UserFile:  NewUserFileClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		UserAsset.
//		Query().
//		Count(ctx)
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	c.UserAsset.Use(hooks...)
	c.UserFile.Use(hooks...)
}

// Intercept adds the query interceptors to all the entity clients.
// In order to add interceptors to a specific client, call: `client.Node.Intercept(...)`.
func (c *Client) Intercept(interceptors ...Interceptor) {
	c.UserAsset.Intercept(interceptors...)
	c.UserFile.Intercept(interceptors...)
}

// Mutate implements the ent.Mutator interface.
func (c *Client) Mutate(ctx context.Context, m Mutation) (Value, error) {
	switch m := m.(type) {
	case *UserAssetMutation:
		return c.UserAsset.mutate(ctx, m)
	case *UserFileMutation:
		return c.UserFile.mutate(ctx, m)
	default:
		return nil, fmt.Errorf("ent: unknown mutation type %T", m)
	}
}

// UserAssetClient is a client for the UserAsset schema.
type UserAssetClient struct {
	config
}

// NewUserAssetClient returns a client for the UserAsset from the given config.
func NewUserAssetClient(c config) *UserAssetClient {
	return &UserAssetClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `userasset.Hooks(f(g(h())))`.
func (c *UserAssetClient) Use(hooks ...Hook) {
	c.hooks.UserAsset = append(c.hooks.UserAsset, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `userasset.Intercept(f(g(h())))`.
func (c *UserAssetClient) Intercept(interceptors ...Interceptor) {
	c.inters.UserAsset = append(c.inters.UserAsset, interceptors...)
}

// Create returns a builder for creating a UserAsset entity.
func (c *UserAssetClient) Create() *UserAssetCreate {
	mutation := newUserAssetMutation(c.config, OpCreate)
	return &UserAssetCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of UserAsset entities.
func (c *UserAssetClient) CreateBulk(builders ...*UserAssetCreate) *UserAssetCreateBulk {
	return &UserAssetCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for UserAsset.
func (c *UserAssetClient) Update() *UserAssetUpdate {
	mutation := newUserAssetMutation(c.config, OpUpdate)
	return &UserAssetUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *UserAssetClient) UpdateOne(ua *UserAsset) *UserAssetUpdateOne {
	mutation := newUserAssetMutation(c.config, OpUpdateOne, withUserAsset(ua))
	return &UserAssetUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *UserAssetClient) UpdateOneID(id int64) *UserAssetUpdateOne {
	mutation := newUserAssetMutation(c.config, OpUpdateOne, withUserAssetID(id))
	return &UserAssetUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for UserAsset.
func (c *UserAssetClient) Delete() *UserAssetDelete {
	mutation := newUserAssetMutation(c.config, OpDelete)
	return &UserAssetDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *UserAssetClient) DeleteOne(ua *UserAsset) *UserAssetDeleteOne {
	return c.DeleteOneID(ua.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *UserAssetClient) DeleteOneID(id int64) *UserAssetDeleteOne {
	builder := c.Delete().Where(userasset.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &UserAssetDeleteOne{builder}
}

// Query returns a query builder for UserAsset.
func (c *UserAssetClient) Query() *UserAssetQuery {
	return &UserAssetQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeUserAsset},
		inters: c.Interceptors(),
	}
}

// Get returns a UserAsset entity by its id.
func (c *UserAssetClient) Get(ctx context.Context, id int64) (*UserAsset, error) {
	return c.Query().Where(userasset.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *UserAssetClient) GetX(ctx context.Context, id int64) *UserAsset {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *UserAssetClient) Hooks() []Hook {
	return c.hooks.UserAsset
}

// Interceptors returns the client interceptors.
func (c *UserAssetClient) Interceptors() []Interceptor {
	return c.inters.UserAsset
}

func (c *UserAssetClient) mutate(ctx context.Context, m *UserAssetMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&UserAssetCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&UserAssetUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&UserAssetUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&UserAssetDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown UserAsset mutation op: %q", m.Op())
	}
}

// UserFileClient is a client for the UserFile schema.
type UserFileClient struct {
	config
}

// NewUserFileClient returns a client for the UserFile from the given config.
func NewUserFileClient(c config) *UserFileClient {
	return &UserFileClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `userfile.Hooks(f(g(h())))`.
func (c *UserFileClient) Use(hooks ...Hook) {
	c.hooks.UserFile = append(c.hooks.UserFile, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `userfile.Intercept(f(g(h())))`.
func (c *UserFileClient) Intercept(interceptors ...Interceptor) {
	c.inters.UserFile = append(c.inters.UserFile, interceptors...)
}

// Create returns a builder for creating a UserFile entity.
func (c *UserFileClient) Create() *UserFileCreate {
	mutation := newUserFileMutation(c.config, OpCreate)
	return &UserFileCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of UserFile entities.
func (c *UserFileClient) CreateBulk(builders ...*UserFileCreate) *UserFileCreateBulk {
	return &UserFileCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for UserFile.
func (c *UserFileClient) Update() *UserFileUpdate {
	mutation := newUserFileMutation(c.config, OpUpdate)
	return &UserFileUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *UserFileClient) UpdateOne(uf *UserFile) *UserFileUpdateOne {
	mutation := newUserFileMutation(c.config, OpUpdateOne, withUserFile(uf))
	return &UserFileUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *UserFileClient) UpdateOneID(id int64) *UserFileUpdateOne {
	mutation := newUserFileMutation(c.config, OpUpdateOne, withUserFileID(id))
	return &UserFileUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for UserFile.
func (c *UserFileClient) Delete() *UserFileDelete {
	mutation := newUserFileMutation(c.config, OpDelete)
	return &UserFileDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *UserFileClient) DeleteOne(uf *UserFile) *UserFileDeleteOne {
	return c.DeleteOneID(uf.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *UserFileClient) DeleteOneID(id int64) *UserFileDeleteOne {
	builder := c.Delete().Where(userfile.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &UserFileDeleteOne{builder}
}

// Query returns a query builder for UserFile.
func (c *UserFileClient) Query() *UserFileQuery {
	return &UserFileQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeUserFile},
		inters: c.Interceptors(),
	}
}

// Get returns a UserFile entity by its id.
func (c *UserFileClient) Get(ctx context.Context, id int64) (*UserFile, error) {
	return c.Query().Where(userfile.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *UserFileClient) GetX(ctx context.Context, id int64) *UserFile {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *UserFileClient) Hooks() []Hook {
	return c.hooks.UserFile
}

// Interceptors returns the client interceptors.
func (c *UserFileClient) Interceptors() []Interceptor {
	return c.inters.UserFile
}

func (c *UserFileClient) mutate(ctx context.Context, m *UserFileMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&UserFileCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&UserFileUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&UserFileUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&UserFileDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown UserFile mutation op: %q", m.Op())
	}
}

// hooks and interceptors per client, for fast access.
type (
	hooks struct {
		UserAsset, UserFile []ent.Hook
	}
	inters struct {
		UserAsset, UserFile []ent.Interceptor
	}
)
