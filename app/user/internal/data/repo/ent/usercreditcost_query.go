// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"store/app/user/internal/data/repo/ent/predicate"
	"store/app/user/internal/data/repo/ent/usercreditcost"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserCreditCostQuery is the builder for querying UserCreditCost entities.
type UserCreditCostQuery struct {
	config
	ctx        *QueryContext
	order      []usercreditcost.OrderOption
	inters     []Interceptor
	predicates []predicate.UserCreditCost
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserCreditCostQuery builder.
func (uccq *UserCreditCostQuery) Where(ps ...predicate.UserCreditCost) *UserCreditCostQuery {
	uccq.predicates = append(uccq.predicates, ps...)
	return uccq
}

// Limit the number of records to be returned by this query.
func (uccq *UserCreditCostQuery) Limit(limit int) *UserCreditCostQuery {
	uccq.ctx.Limit = &limit
	return uccq
}

// Offset to start from.
func (uccq *UserCreditCostQuery) Offset(offset int) *UserCreditCostQuery {
	uccq.ctx.Offset = &offset
	return uccq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uccq *UserCreditCostQuery) Unique(unique bool) *UserCreditCostQuery {
	uccq.ctx.Unique = &unique
	return uccq
}

// Order specifies how the records should be ordered.
func (uccq *UserCreditCostQuery) Order(o ...usercreditcost.OrderOption) *UserCreditCostQuery {
	uccq.order = append(uccq.order, o...)
	return uccq
}

// First returns the first UserCreditCost entity from the query.
// Returns a *NotFoundError when no UserCreditCost was found.
func (uccq *UserCreditCostQuery) First(ctx context.Context) (*UserCreditCost, error) {
	nodes, err := uccq.Limit(1).All(setContextOp(ctx, uccq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usercreditcost.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uccq *UserCreditCostQuery) FirstX(ctx context.Context) *UserCreditCost {
	node, err := uccq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserCreditCost ID from the query.
// Returns a *NotFoundError when no UserCreditCost ID was found.
func (uccq *UserCreditCostQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = uccq.Limit(1).IDs(setContextOp(ctx, uccq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{usercreditcost.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uccq *UserCreditCostQuery) FirstIDX(ctx context.Context) int64 {
	id, err := uccq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserCreditCost entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserCreditCost entity is found.
// Returns a *NotFoundError when no UserCreditCost entities are found.
func (uccq *UserCreditCostQuery) Only(ctx context.Context) (*UserCreditCost, error) {
	nodes, err := uccq.Limit(2).All(setContextOp(ctx, uccq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usercreditcost.Label}
	default:
		return nil, &NotSingularError{usercreditcost.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uccq *UserCreditCostQuery) OnlyX(ctx context.Context) *UserCreditCost {
	node, err := uccq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserCreditCost ID in the query.
// Returns a *NotSingularError when more than one UserCreditCost ID is found.
// Returns a *NotFoundError when no entities are found.
func (uccq *UserCreditCostQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = uccq.Limit(2).IDs(setContextOp(ctx, uccq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{usercreditcost.Label}
	default:
		err = &NotSingularError{usercreditcost.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uccq *UserCreditCostQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := uccq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserCreditCosts.
func (uccq *UserCreditCostQuery) All(ctx context.Context) ([]*UserCreditCost, error) {
	ctx = setContextOp(ctx, uccq.ctx, "All")
	if err := uccq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserCreditCost, *UserCreditCostQuery]()
	return withInterceptors[[]*UserCreditCost](ctx, uccq, qr, uccq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uccq *UserCreditCostQuery) AllX(ctx context.Context) []*UserCreditCost {
	nodes, err := uccq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserCreditCost IDs.
func (uccq *UserCreditCostQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if uccq.ctx.Unique == nil && uccq.path != nil {
		uccq.Unique(true)
	}
	ctx = setContextOp(ctx, uccq.ctx, "IDs")
	if err = uccq.Select(usercreditcost.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uccq *UserCreditCostQuery) IDsX(ctx context.Context) []int64 {
	ids, err := uccq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uccq *UserCreditCostQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uccq.ctx, "Count")
	if err := uccq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uccq, querierCount[*UserCreditCostQuery](), uccq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uccq *UserCreditCostQuery) CountX(ctx context.Context) int {
	count, err := uccq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uccq *UserCreditCostQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uccq.ctx, "Exist")
	switch _, err := uccq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uccq *UserCreditCostQuery) ExistX(ctx context.Context) bool {
	exist, err := uccq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserCreditCostQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uccq *UserCreditCostQuery) Clone() *UserCreditCostQuery {
	if uccq == nil {
		return nil
	}
	return &UserCreditCostQuery{
		config:     uccq.config,
		ctx:        uccq.ctx.Clone(),
		order:      append([]usercreditcost.OrderOption{}, uccq.order...),
		inters:     append([]Interceptor{}, uccq.inters...),
		predicates: append([]predicate.UserCreditCost{}, uccq.predicates...),
		// clone intermediate query.
		sql:  uccq.sql.Clone(),
		path: uccq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserCreditCost.Query().
//		GroupBy(usercreditcost.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (uccq *UserCreditCostQuery) GroupBy(field string, fields ...string) *UserCreditCostGroupBy {
	uccq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserCreditCostGroupBy{build: uccq}
	grbuild.flds = &uccq.ctx.Fields
	grbuild.label = usercreditcost.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.UserCreditCost.Query().
//		Select(usercreditcost.FieldCreatedAt).
//		Scan(ctx, &v)
func (uccq *UserCreditCostQuery) Select(fields ...string) *UserCreditCostSelect {
	uccq.ctx.Fields = append(uccq.ctx.Fields, fields...)
	sbuild := &UserCreditCostSelect{UserCreditCostQuery: uccq}
	sbuild.label = usercreditcost.Label
	sbuild.flds, sbuild.scan = &uccq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserCreditCostSelect configured with the given aggregations.
func (uccq *UserCreditCostQuery) Aggregate(fns ...AggregateFunc) *UserCreditCostSelect {
	return uccq.Select().Aggregate(fns...)
}

func (uccq *UserCreditCostQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uccq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uccq); err != nil {
				return err
			}
		}
	}
	for _, f := range uccq.ctx.Fields {
		if !usercreditcost.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if uccq.path != nil {
		prev, err := uccq.path(ctx)
		if err != nil {
			return err
		}
		uccq.sql = prev
	}
	return nil
}

func (uccq *UserCreditCostQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserCreditCost, error) {
	var (
		nodes = []*UserCreditCost{}
		_spec = uccq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserCreditCost).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserCreditCost{config: uccq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uccq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (uccq *UserCreditCostQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uccq.querySpec()
	_spec.Node.Columns = uccq.ctx.Fields
	if len(uccq.ctx.Fields) > 0 {
		_spec.Unique = uccq.ctx.Unique != nil && *uccq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uccq.driver, _spec)
}

func (uccq *UserCreditCostQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(usercreditcost.Table, usercreditcost.Columns, sqlgraph.NewFieldSpec(usercreditcost.FieldID, field.TypeInt64))
	_spec.From = uccq.sql
	if unique := uccq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uccq.path != nil {
		_spec.Unique = true
	}
	if fields := uccq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, usercreditcost.FieldID)
		for i := range fields {
			if fields[i] != usercreditcost.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := uccq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uccq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uccq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uccq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uccq *UserCreditCostQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uccq.driver.Dialect())
	t1 := builder.Table(usercreditcost.Table)
	columns := uccq.ctx.Fields
	if len(columns) == 0 {
		columns = usercreditcost.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uccq.sql != nil {
		selector = uccq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uccq.ctx.Unique != nil && *uccq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range uccq.predicates {
		p(selector)
	}
	for _, p := range uccq.order {
		p(selector)
	}
	if offset := uccq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uccq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserCreditCostGroupBy is the group-by builder for UserCreditCost entities.
type UserCreditCostGroupBy struct {
	selector
	build *UserCreditCostQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uccgb *UserCreditCostGroupBy) Aggregate(fns ...AggregateFunc) *UserCreditCostGroupBy {
	uccgb.fns = append(uccgb.fns, fns...)
	return uccgb
}

// Scan applies the selector query and scans the result into the given value.
func (uccgb *UserCreditCostGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uccgb.build.ctx, "GroupBy")
	if err := uccgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserCreditCostQuery, *UserCreditCostGroupBy](ctx, uccgb.build, uccgb, uccgb.build.inters, v)
}

func (uccgb *UserCreditCostGroupBy) sqlScan(ctx context.Context, root *UserCreditCostQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uccgb.fns))
	for _, fn := range uccgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uccgb.flds)+len(uccgb.fns))
		for _, f := range *uccgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uccgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uccgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserCreditCostSelect is the builder for selecting fields of UserCreditCost entities.
type UserCreditCostSelect struct {
	*UserCreditCostQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (uccs *UserCreditCostSelect) Aggregate(fns ...AggregateFunc) *UserCreditCostSelect {
	uccs.fns = append(uccs.fns, fns...)
	return uccs
}

// Scan applies the selector query and scans the result into the given value.
func (uccs *UserCreditCostSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uccs.ctx, "Select")
	if err := uccs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserCreditCostQuery, *UserCreditCostSelect](ctx, uccs.UserCreditCostQuery, uccs, uccs.inters, v)
}

func (uccs *UserCreditCostSelect) sqlScan(ctx context.Context, root *UserCreditCostQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(uccs.fns))
	for _, fn := range uccs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*uccs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uccs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
