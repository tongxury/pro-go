// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"store/app/user/internal/data/repo/ent/membersubscribe"
	"store/app/user/internal/data/repo/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// MemberSubscribeQuery is the builder for querying MemberSubscribe entities.
type MemberSubscribeQuery struct {
	config
	ctx        *QueryContext
	order      []membersubscribe.OrderOption
	inters     []Interceptor
	predicates []predicate.MemberSubscribe
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MemberSubscribeQuery builder.
func (msq *MemberSubscribeQuery) Where(ps ...predicate.MemberSubscribe) *MemberSubscribeQuery {
	msq.predicates = append(msq.predicates, ps...)
	return msq
}

// Limit the number of records to be returned by this query.
func (msq *MemberSubscribeQuery) Limit(limit int) *MemberSubscribeQuery {
	msq.ctx.Limit = &limit
	return msq
}

// Offset to start from.
func (msq *MemberSubscribeQuery) Offset(offset int) *MemberSubscribeQuery {
	msq.ctx.Offset = &offset
	return msq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (msq *MemberSubscribeQuery) Unique(unique bool) *MemberSubscribeQuery {
	msq.ctx.Unique = &unique
	return msq
}

// Order specifies how the records should be ordered.
func (msq *MemberSubscribeQuery) Order(o ...membersubscribe.OrderOption) *MemberSubscribeQuery {
	msq.order = append(msq.order, o...)
	return msq
}

// First returns the first MemberSubscribe entity from the query.
// Returns a *NotFoundError when no MemberSubscribe was found.
func (msq *MemberSubscribeQuery) First(ctx context.Context) (*MemberSubscribe, error) {
	nodes, err := msq.Limit(1).All(setContextOp(ctx, msq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{membersubscribe.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (msq *MemberSubscribeQuery) FirstX(ctx context.Context) *MemberSubscribe {
	node, err := msq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MemberSubscribe ID from the query.
// Returns a *NotFoundError when no MemberSubscribe ID was found.
func (msq *MemberSubscribeQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = msq.Limit(1).IDs(setContextOp(ctx, msq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{membersubscribe.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (msq *MemberSubscribeQuery) FirstIDX(ctx context.Context) int64 {
	id, err := msq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MemberSubscribe entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MemberSubscribe entity is found.
// Returns a *NotFoundError when no MemberSubscribe entities are found.
func (msq *MemberSubscribeQuery) Only(ctx context.Context) (*MemberSubscribe, error) {
	nodes, err := msq.Limit(2).All(setContextOp(ctx, msq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{membersubscribe.Label}
	default:
		return nil, &NotSingularError{membersubscribe.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (msq *MemberSubscribeQuery) OnlyX(ctx context.Context) *MemberSubscribe {
	node, err := msq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MemberSubscribe ID in the query.
// Returns a *NotSingularError when more than one MemberSubscribe ID is found.
// Returns a *NotFoundError when no entities are found.
func (msq *MemberSubscribeQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = msq.Limit(2).IDs(setContextOp(ctx, msq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{membersubscribe.Label}
	default:
		err = &NotSingularError{membersubscribe.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (msq *MemberSubscribeQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := msq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MemberSubscribes.
func (msq *MemberSubscribeQuery) All(ctx context.Context) ([]*MemberSubscribe, error) {
	ctx = setContextOp(ctx, msq.ctx, "All")
	if err := msq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MemberSubscribe, *MemberSubscribeQuery]()
	return withInterceptors[[]*MemberSubscribe](ctx, msq, qr, msq.inters)
}

// AllX is like All, but panics if an error occurs.
func (msq *MemberSubscribeQuery) AllX(ctx context.Context) []*MemberSubscribe {
	nodes, err := msq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MemberSubscribe IDs.
func (msq *MemberSubscribeQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if msq.ctx.Unique == nil && msq.path != nil {
		msq.Unique(true)
	}
	ctx = setContextOp(ctx, msq.ctx, "IDs")
	if err = msq.Select(membersubscribe.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (msq *MemberSubscribeQuery) IDsX(ctx context.Context) []int64 {
	ids, err := msq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (msq *MemberSubscribeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, msq.ctx, "Count")
	if err := msq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, msq, querierCount[*MemberSubscribeQuery](), msq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (msq *MemberSubscribeQuery) CountX(ctx context.Context) int {
	count, err := msq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (msq *MemberSubscribeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, msq.ctx, "Exist")
	switch _, err := msq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (msq *MemberSubscribeQuery) ExistX(ctx context.Context) bool {
	exist, err := msq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MemberSubscribeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (msq *MemberSubscribeQuery) Clone() *MemberSubscribeQuery {
	if msq == nil {
		return nil
	}
	return &MemberSubscribeQuery{
		config:     msq.config,
		ctx:        msq.ctx.Clone(),
		order:      append([]membersubscribe.OrderOption{}, msq.order...),
		inters:     append([]Interceptor{}, msq.inters...),
		predicates: append([]predicate.MemberSubscribe{}, msq.predicates...),
		// clone intermediate query.
		sql:  msq.sql.Clone(),
		path: msq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int64 `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MemberSubscribe.Query().
//		GroupBy(membersubscribe.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (msq *MemberSubscribeQuery) GroupBy(field string, fields ...string) *MemberSubscribeGroupBy {
	msq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MemberSubscribeGroupBy{build: msq}
	grbuild.flds = &msq.ctx.Fields
	grbuild.label = membersubscribe.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int64 `json:"user_id,omitempty"`
//	}
//
//	client.MemberSubscribe.Query().
//		Select(membersubscribe.FieldUserID).
//		Scan(ctx, &v)
func (msq *MemberSubscribeQuery) Select(fields ...string) *MemberSubscribeSelect {
	msq.ctx.Fields = append(msq.ctx.Fields, fields...)
	sbuild := &MemberSubscribeSelect{MemberSubscribeQuery: msq}
	sbuild.label = membersubscribe.Label
	sbuild.flds, sbuild.scan = &msq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MemberSubscribeSelect configured with the given aggregations.
func (msq *MemberSubscribeQuery) Aggregate(fns ...AggregateFunc) *MemberSubscribeSelect {
	return msq.Select().Aggregate(fns...)
}

func (msq *MemberSubscribeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range msq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, msq); err != nil {
				return err
			}
		}
	}
	for _, f := range msq.ctx.Fields {
		if !membersubscribe.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if msq.path != nil {
		prev, err := msq.path(ctx)
		if err != nil {
			return err
		}
		msq.sql = prev
	}
	return nil
}

func (msq *MemberSubscribeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MemberSubscribe, error) {
	var (
		nodes = []*MemberSubscribe{}
		_spec = msq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MemberSubscribe).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MemberSubscribe{config: msq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, msq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (msq *MemberSubscribeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := msq.querySpec()
	_spec.Node.Columns = msq.ctx.Fields
	if len(msq.ctx.Fields) > 0 {
		_spec.Unique = msq.ctx.Unique != nil && *msq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, msq.driver, _spec)
}

func (msq *MemberSubscribeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(membersubscribe.Table, membersubscribe.Columns, sqlgraph.NewFieldSpec(membersubscribe.FieldID, field.TypeInt64))
	_spec.From = msq.sql
	if unique := msq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if msq.path != nil {
		_spec.Unique = true
	}
	if fields := msq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, membersubscribe.FieldID)
		for i := range fields {
			if fields[i] != membersubscribe.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := msq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := msq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := msq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := msq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (msq *MemberSubscribeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(msq.driver.Dialect())
	t1 := builder.Table(membersubscribe.Table)
	columns := msq.ctx.Fields
	if len(columns) == 0 {
		columns = membersubscribe.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if msq.sql != nil {
		selector = msq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if msq.ctx.Unique != nil && *msq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range msq.predicates {
		p(selector)
	}
	for _, p := range msq.order {
		p(selector)
	}
	if offset := msq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := msq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MemberSubscribeGroupBy is the group-by builder for MemberSubscribe entities.
type MemberSubscribeGroupBy struct {
	selector
	build *MemberSubscribeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (msgb *MemberSubscribeGroupBy) Aggregate(fns ...AggregateFunc) *MemberSubscribeGroupBy {
	msgb.fns = append(msgb.fns, fns...)
	return msgb
}

// Scan applies the selector query and scans the result into the given value.
func (msgb *MemberSubscribeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, msgb.build.ctx, "GroupBy")
	if err := msgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MemberSubscribeQuery, *MemberSubscribeGroupBy](ctx, msgb.build, msgb, msgb.build.inters, v)
}

func (msgb *MemberSubscribeGroupBy) sqlScan(ctx context.Context, root *MemberSubscribeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(msgb.fns))
	for _, fn := range msgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*msgb.flds)+len(msgb.fns))
		for _, f := range *msgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*msgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := msgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MemberSubscribeSelect is the builder for selecting fields of MemberSubscribe entities.
type MemberSubscribeSelect struct {
	*MemberSubscribeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mss *MemberSubscribeSelect) Aggregate(fns ...AggregateFunc) *MemberSubscribeSelect {
	mss.fns = append(mss.fns, fns...)
	return mss
}

// Scan applies the selector query and scans the result into the given value.
func (mss *MemberSubscribeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mss.ctx, "Select")
	if err := mss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MemberSubscribeQuery, *MemberSubscribeSelect](ctx, mss.MemberSubscribeQuery, mss, mss.inters, v)
}

func (mss *MemberSubscribeSelect) sqlScan(ctx context.Context, root *MemberSubscribeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mss.fns))
	for _, fn := range mss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
